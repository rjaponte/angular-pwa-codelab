import { FuzzyTimePipe } from './fuzzy-time.pipe';

describe('FuzzyTimePipe', () => {
  it('create an instance', () => {
    const pipe = new FuzzyTimePipe();
    expect(pipe).toBeTruthy();
  });
});
